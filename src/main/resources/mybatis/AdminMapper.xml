<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.AdminMapper">
    <insert id="intoNotice" parameterType="map">
        insert into notice(title, text, time) value (#{title}, #{text}, #{time})
    </insert>


    <select id="selectAdminById" resultType="com.model.Admin">
        select id, password, name, sex, phone
        from admin
        where id = #{id};
    </select>
    <select id="findByLimit" parameterType="map" resultType="map">
        select title, text, time
        from notice
        limit #{startIndex},#{pageSize}
    </select>
</mapper>
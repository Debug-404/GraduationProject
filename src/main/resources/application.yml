spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bishe?useSSL=true&amp;useUnicode=true&amp;characterEncoding=utf8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  type-aliases-package: com.pojo
  mapper-locations: classpath:/mybatis/*.xml
logging:
  level:
    root: info
    com: debug
